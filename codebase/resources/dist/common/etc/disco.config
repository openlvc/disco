# Disco Configuration File
disco.log.level = INFO
disco.log.console = off
disco.log.file = disco.log

disco.connection = udp

disco.udp.interface = SITE_LOCAL
disco.udp.address = BROADCAST
disco.udp.port = 3000